# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

[project]
name = "nhx-automodel"
description = "NeMo Automodel job compiler and platform tasks (no HTTP server)."
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = [
    "nhx-common",
    "nhx-customization-common",
    "nemo-helix-plugin",
    "pydantic>=2.10.6",
    "pydantic-settings>=2.6.1",
    "httpx>=0.27.0",
    "aiofiles>=24.1.0",
    "tenacity>=8.5.0",
    "jsonschema>=4.23.0",
]
version = "0.0.0"

[project.optional-dependencies]
# Container-only: checkpoints.py imports peft to merge LoRA adapters. Keeping it out of the base
# deps leaves plugin discovery and the compiler torch-free, and the >=0.20 floor avoids peft's
# import of torchao's removed LinearActivationQuantizedTensor.
training = ["peft>=0.20.0"]
dev = ["pytest>=8.3.4", "pytest-asyncio>=0.25.3", "pytest-mock>=3.14.0"]

[project.scripts]
nhx-automodel-training = "nhx.automodel.tasks.training.__main__:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nhx"]

[tool.uv.sources]
nhx-common = { workspace = true }
nhx-customization-common = { workspace = true }
nemo-helix-plugin = { workspace = true }

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
testpaths = ["tests"]
